pipeline {
    agent { dockerfile true }
    stages {
        stage('Building Image on the fly !!!!') {
            steps {
                sh 'docker --version'
                sh 'java --version'
            }
        }
    }
}
